/**
 * 测试接口
 * @module test
 * @author zhanghuan
 * @date 2021/01/30
 */

import { prefix, mockPrefix } from './config'

export default {
    getData: `${prefix}/openapi/v1/migration/rank`,
    menu: `${mockPrefix}/menu`,
}